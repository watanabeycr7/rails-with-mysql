<h1>index</h1>
<p><%= link_to '新規作成', new_tweet_path %></p>
<table>
<% @tweets.each do |tweet| %>
    <tr>
        <td><%= tweet.text %></td>
        <td><%=  link_to '詳細', tweet_path(tweet.id), method: :get%></td>
    </tr>
<% end %>
</table>